contract counter {

    function addnumbers(uint64 x, uint64 y) public returns (uint64){
        return x+y;
    }

    function subtractnumbers(uint64 a, uint64 b) public returns (uint64){
        return a-b;
    }

}
